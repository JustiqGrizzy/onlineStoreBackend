server:
  port: 1111
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onlineStore
    username: postgres
    password: 1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true


